一、cartographer 的主要优势是采用了偏向于工程应用的SLAM算法，包括Scan和Submap两部分，Scan-to-scan和scan-to-map，
先使用一部分得到submap，作为关键帧，然后再对submap进行Loop closure optimization（闭环优化）。消除累计误差。
关键部分在于如何选取关键帧submap，以及如何优化闭环检测的内容。
二、Cartograhper主要从2个方面入手处理这些图相关的问题：
采用2分级的图结构,整个地图分为多个子图，而每个子图表示为占用格结构，新Scan只在子图内处理以保证快速和稳定的处理时间，
另一方面，在检测到回环时通过对包括所有子图姿势的全图进行优化求解以消除Scan和子图匹配所引入的累计误差问题，
这里由于不是对所有Scan进行处理，而是以子图为单位进行，使得整个的优化求解时间极大的缩小。
回环相关的性能优化，比较直接的做法通过给予当前位置的估计值进行相邻范围内的回环检测，
这种方法和当前位置的估计精度相关，或是通过缩小地图进行粗糙地匹配，此处Cartograhper采用分支界限+DFS搜索的策略以进行回环的检测和相对位姿求解，
相比传统的先检测回环，在求解相对位姿来说，显得结构上更为统一，将建立回环的过程转化为一个查找过程，
而另一方面，当完成对离散候选解空间的树形构造后，在树中对解的查找过程速度很快，剩下的对于建树过程中的节点的Bound的选择通过对子图的precompute来完成，
这种中间结构的引入（代价O(n)）使得回环过程可以实时的完成，从而通过回环不断调整子图以消除累计误差。
所以，Cartographer可以说是从工程的角度入手为2D-SLAM在适中的硬件上运行提供了一个较好的范例。
三、
https://pic3.zhimg.com/80/v2-7549fd48914c60ce5a8f5e6fb02616e0_hd.jpg      //图片链接展示的是cartographer的整个结构
branch and bound：分支定界。将该方法引入了闭环检测
Iterative Closest Point（ICP）迭代最近点。一种点云匹配算法，有各种各样的变形。它是基于纯粹几何模型的三维物体对准算法。
